const isActive = true
const hasPermission = false

// conversion implicita de cualquier tipo de dato  a boobleano
const result = 5 > 4
console.log(result)

const name = "Platzi"
console.log(!!name)  //verificar si contiene algun valor

// conversion explicita a boolean
const value = 0
const explicitBoolean = Boolean(value)
console.log(explicitBoolean)